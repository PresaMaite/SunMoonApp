import { BrowserRouter, Route, Switch } from "react-router-dom";
import { Layout } from "../layouts/layout";

export const Routes = () => (
    <BrowserRouter>

        <Layout>
            
            <Switch>
                <Route></Route>
            </Switch>
            

        </Layout>

    
    </BrowserRouter>
)